using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class soundVolumeScript : MonoBehaviour
{
    //We now use an AudioMixer
   /* public FloatVariable globalVolume;
    public FloatVariable soundVolume;
    // Start is called before the first frame update
    void Start()
    {
        this.GetComponent<AudioSource>().volume = globalVolume.value * soundVolume.value;
    }*/

}
